[package]
name = "wallpaper-shuffler-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
rand = "0.8"

[dev-dependencies]
tempfile = "3"
